# Copy this file to .env and set values for local development.
# For production builds, set these via EAS Secrets or your CI/CD platform.

# "development" bypasses email verification on sign-up/sign-in.
# "production" enforces it.
EXPO_PUBLIC_APP_ENV=development
